DISCORD_TOKEN=replace-with-your-bot-token
COMMAND_PREFIX=!

# Comma-separated Discord user IDs allowed to run !reload.
# Server administrators are always allowed. Use * only for a fully trusted server.
RELOAD_USER_IDS=
